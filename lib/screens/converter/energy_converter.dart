import 'package:flutter/material.dart';

class EnergyConverter extends StatelessWidget {
  const EnergyConverter({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('能量转换器'),
    );
  }
}
